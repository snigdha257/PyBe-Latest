const User = require('./User');
const LearningPath = require('./LearningPath');
const Module = require('./Module');
const UserProgress = require('./UserProgress');

module.exports = { User, LearningPath, Module, UserProgress };
